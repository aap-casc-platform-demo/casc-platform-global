---
# Example organizations configuration — rename to .yml to activate.
# This file belongs in base/organizations/ and applies to ALL environments.
# For environment-specific overrides, place a .yml file in <env>/organizations/.
aap_organizations:
  - name: "Example Organization"
    description: "Example organization — replace with your organization"
